{
  "name": "qr-platform-root",
  "private": true,
  "description": "Repository root. The application frontend lives in ./frontend and has its own package.json. This file only holds dev/debug script dependencies.",
  "dependencies": {
    "axios": "^1.7.9"
  }
}
